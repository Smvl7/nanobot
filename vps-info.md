# VPS Environment Info

Этот файл предназначен для смежных команд и агентов, которые планируют деплоить сервисы на этот же сервер.

## 🌍 Server Info
- **IP Address:** `85.208.86.93`
- **OS:** Ubuntu (предположительно 22.04/24.04)
- **User:** см. SSH конфиг владельца

## 🏙 Current Residents (Services)

### 1. Pupi Coffee Bot (Project: `pupin-cupping`)
- **Type:** Telegram Bot + Web Dashboard
- **Deploy Path:** `~/pupin-cupping` (или аналогичный в домашней директории)
- **Docker Compose:** Используется.
- **Network:** Bridge mode (стандартный).

### ⛔️ Reserved Ports (Occupied)
Пожалуйста, **НЕ используйте** эти порты для новых сервисов, чтобы избежать конфликтов:

| Port (Host) | Service | Description |
|---|---|---|
| **8000** | `pupi-web` | Web Dashboard (FastAPI) |
| **5432** | *(Potential)* | Если поднимется общий Postgres (сейчас используется SQLite внутри контейнера) |

*> Pupi Bot (Telegram) не занимает входящих портов (использует Long Polling).*

## 🤝 Rules for Neighbors (Guidelines)

1.  **Docker Only:** Пожалуйста, запускайте свои сервисы тоже в Docker контейнерах.
2.  **Separate Directories:** Держите свой `docker-compose.yml` в отдельной папке (например, `~/new-bot-name`).
3.  **Port Allocation:**
    - Если вам нужен веб-интерфейс, используйте свободные порты (например: `8001`, `8080`, `3000`).
    - Проверьте занятость порта на сервере перед запуском: `sudo netstat -tulpn | grep LISTEN`
4.  **Resources:** Сервер не резиновый. Если ваш бот майнит крипту или гоняет LLM локально (Ollama), Pupi может начать тормозить. Pupi использует внешние API (OpenRouter), поэтому он легкий.

## 📞 Contact
По вопросам конфликтов портов пишите владельцу сервера (Samvel).
